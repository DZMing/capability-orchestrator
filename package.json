{
  "name": "capability-orchestrator",
  "version": "1.0.0",
  "description": "能力感知层：实时扫描 Claude Code 环境中所有可用 skills、subagents、plugins 和 MCP servers",
  "scripts": {
    "test": "node --test tests/scan.test.cjs tests/skill-contract.test.cjs",
    "test:install": "bash tests/install.test.sh",
    "test:idempotent": "bash tests/install-idempotent.test.sh",
    "test:all": "npm test && npm run test:install && npm run test:idempotent"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/DZMing/capability-orchestrator.git"
  },
  "author": "Zhiming Deng"
}
